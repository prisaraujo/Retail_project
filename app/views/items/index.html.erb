<!DOCTYPE html>
<html>
  <head>
    <style type="text/css" src="././assets/stylesheets/bootstrap.css"></style>
    <style type="text/css" src="././assets/stylesheets/application.css"></style>
  </head>

  <div class="container-custom">
    <h1 class="page-header">ThoughtWorks Retail Dinners</h1>
      Search: <input type="text" id="search" onkeyup="filter_table()" placeholder="Type to search"> <br /><br>
      <table class="table-striped table-bordered" id="table">
        <thead>
          <tr id="dinner_labels">
            <th>Dinner title</th>
            <th>City</th>
            <th>Attended/No Show</th>
            <th>Follow-up owned by</th>
            <th>Follow-up status</th>
            <th>Comments</th>
            <th>Name</th>
            <th>Company</th>
            <th>Role</th>
            <th>E-mail address</th>
            <th>Address</th>
            <th>Phone</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <div class="items">
            <% @items.each do |item| %>
              <tr id="pad">
                <td><%= item.title %></td>
                <td><%= item.city %></td>
                <td><%= item.attendence %></td>
                <td><%= item.followup_ownedby %></td>
                <td><%= item.followup_status %></td>
                <td><%= item.comments %></td>
                <td><%= item.name %></td>
                <td><%= item.company %></td>
                <td><%= item.role %></td>
                <td><%= item.emailaddress %></td>
                <td><%= item.address %></td>
                <td><%= item.phone %></td>
                <td><%= link_to 'Show', item %></td>
                <td><%= link_to 'Edit', edit_item_path(item) %></td>
                <td><%= link_to 'Delete', item, method: :delete, data: { confirm: 'Are you sure you want to delete?' } %></td>
              </tr>
            <% end %>
          </div>
        </tbody>
    </table>
    <button type="button" class="btn btn-primary" id="button-custom">
      <%= link_to 'New Item', new_item_path %>
    </button>
  </div>
  <br>
</html>
