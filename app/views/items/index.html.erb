<!DOCTYPE html>
<html>
  <head>
    <style type="text/css" src="././assets/stylesheets/bootstrap.css"></style>
    <style type="text/css" src="././assets/stylesheets/bootstrap.min.css"></style>
    <style type="text/css" src="././assets/stylesheets/application.css"></style>
  </head>

  <div id="menu">
    <ul>
      <li class="active"><a href="">Home</a></li>
      <li><a href="">Profile</a></li>
      <li><a href="">Dinners</a></li>
    </ul>
  </div>

  <div id="sidebar_nav">
    <ul>
      <li></li>
      <li></li>
    </ul>
  </div>

  <h1 id="dinner_title">ThoughtWorks Retail Dinners</h1>
  <table class="DinnersTable">
    <thead>
      <tr id="dinner_labels">
        <th>Dinner title</th>
        <th>City</th>
        <th>Attendence/No Show</th>
        <th>Follow-up owned by</th>
        <th>Follow-up status</th>
        <th>Comments</th>
        <th>Name</th>
        <th>Company</th>
        <th>Role</th>
        <th>E-mail address</th>
        <th>Address</th>
        <th>Phone</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @items.each do |item| %>
        <tr>
          <td><%= item.title %></td>
          <td><%= item.city %></td>
          <td><%= item.attendence %></td>
          <td><%= item.followup_ownedby %></td>
          <td><%= item.followup_status %></td>
          <td><%= item.comments %></td>
          <td><%= item.name %></td>
          <td><%= item.company %></td>
          <td><%= item.role %></td>
          <td><%= item.emailaddress %></td>
          <td><%= item.address %></td>
          <td><%= item.phone %></td>
          <td><%= link_to 'Show', item %></td>
          <td><%= link_to 'Edit', edit_item_path(item) %></td>
          <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Item', new_item_path %>

</html>
